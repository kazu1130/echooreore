name: Echo workflow

on:
  push:
  workflow_dispatch:

jobs:
  echo_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo a message
        run: |
          echo "Hello from GitHub Actions!"
